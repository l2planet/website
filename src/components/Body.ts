import { wrapn } from 'wrapn';

/** Wrapped `<body/>` component. */
export const Body = wrapn('body')`
    font-medium
    leading-tight

    text-gris-9
    dark:text-gris-0
    
    bg-gris-0
    dark:bg-gris-9
`;
